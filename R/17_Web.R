2019-10-24

## WEB
# 데이터를 구축
# Database로 부터 데이터를 얻어올 수 있다.
# Open API를 이용해서 데이터를 얻어올 수 있다.
    # Open API : 공개되어있는 프로그램
# 파일로부터 데이터를 얻어올 수 있다
# 프로그램적으로 WEB scraping, crawling을 통해 데이터를 구축
 
# HTML, CSS, JavaScript
# HTML : 웹페이지의 내용, 구조를 담당
# CSS : 화면에 대한 제어, 스타일링, 화면 구성
# JavaScript : 웹페이지의 동적처리를 담당, 프로그램적 처리, 프로그래밍 언어

# 기본적으로 web은 CS구조로 되어있다
# CS구조 : client-server구조 (페이지를 제공하는 서버 프로그램이 존재)
# server쪽 프로그램 : web server
#                     HTML, CSS, JavaScript를 제공
# client쪽 프로그램 : chrome(browser)
# 서버로부터 HTML, CSS, JavaScript를 받아서 화면에 rendering(그리다)을 한다.

# HTML,CSS,JavaScript를 이용해서 클라이언트에게 제공해주는 web page를 만들어보자
# 개발툴이 있어야 한다
# webstorm을 이요애서 개발해보자

# chrome->webstorm

# 클라이언트에게 제공할 HTML을 작성했다
# 서버프로그램(web server)에게 우리 프로젝트를 알려줘야 한다.
# 우리 프로젝트를 web server에게 인지시켜야 한다 (configure)
# web server 프로그램을 기동시켜서 우리 프로젝트를 웹에 deploy
    # 웹페이지를 웹서버를 통해 띄운다
# 내가 만든 프로젝트는 클라이언트가 와서 받아가서 사용할 수 있다
# web client(browser)를 이용해서 URL(주소, 좌표) 서버쪽 프로그램에 접속해서 (서버가 제공해주는)HTML을 받아가서 화면에 그린다(표시한다)

# web server가 webstorm에 내장되어 있다

# - keyword로 도서검색 후 출력(실습)
# web crawling & web scraping

### HTML, CSS, JavaScript 웹 언어

# W3C
# HTML 이 발전발전해서 1999년 4.01 버젼
# W3C가 1999년 12에 HTML 4.01을 마지막으로 더이상 HTML의 버전업은 없어
# HTML 언어는 기본적으로 2가지 문제점을 가지고 있다

# HTML
# 1. 정형성이 없다 => 유지보수시에 문제점이 발생하게 된다
# 2. 확장성이 없다 => 정해진 tag만 이용해서 사용하다보니 배우고 쓰기는 쉽지만 응용해서 확장하기에는 적합하지 않다.

# 2000년 부터 W3C는 HTML 표준을 다른것을 가미해서 위의 2가지 문제를 해결하려했다
# XML을 HTML에 도입해서 표준으로 끌고 가려했다.
# XHTML 1.0 이 시작
# XML사용에 회의적인 시각을 가지고 있는 몇몇 회사들이 컨소시엄을 구성
# XML대신 HTML을 발전시켜보자 => HTML5
# HTML5의 의의 : 웹페이지 대신 웹 어플리케이션이 대세가 될거야
# 웹페이지 대신 Frond-End web application을사용하게 된다.
# Angular, react.js, view.js => JavaScript












